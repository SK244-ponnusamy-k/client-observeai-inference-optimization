# ==============================================================================
# configs/manifests/gpt-oss-20b-neuron-trn2-bf16.yaml  - GENERATED by `oai model generate gpt-oss-20b-neuron`
#
# Benchmark run manifest for openai/gpt-oss-20b (bf16) on trn2.48xlarge.
# Edit catalog/models/gpt-oss-20b-neuron.yaml and re-generate, or tweak this file directly for
# a one-off run. Every field is read by inference/load-test.py.
# ==============================================================================
run_id: gpt-oss-20b-neuron-2026-09-15-001
description: "openai/gpt-oss-20b bf16, trn2.48xlarge"

model:
  hf_id: openai/gpt-oss-20b
  served_name: gpt-oss-20b
  tokenizer: openai/gpt-oss-20b
  s3_prefix: gpt-oss-20b/
  source: neuron

serving:
  image: public.ecr.aws/neuron/pytorch-inference-vllm-neuronx:0.21.0.1.0.0-neuronx-py313-sdk2.31.0-ubuntu24.04
  nodepool: neuron-inf
  instance_type: trn2.48xlarge

optimization_variants:
  - label: bf16-trn2
    quantization: bf16
    tensor_parallel_size: 8
    max_num_seqs: 4
    max_num_batched_tokens: 8192
    max_model_len: 16384
    kv_cache_dtype: auto

benchmark:
  workload_profiles:
    - configs/workload_profiles/realtime_v1.yaml
    - configs/workload_profiles/batch_v1.yaml

# cost.instance_hourly_usd is resolved DYNAMICALLY from the deployed instance
# against the EC2 price book in load-test.py (trn2.48xlarge = 12.0 there).
# Set an explicit value here ONLY to override. VERIFY the Trainium price before a run.

tags:
  Project: observeai-inference-optimization
  Engagement: shellkode-sow
  Environment: sandbox
  ManagedBy: oai-cli
  Owner: TBD
  CostCenter: TBD
  RunId: gpt-oss-20b-neuron-2026-09-15-001
