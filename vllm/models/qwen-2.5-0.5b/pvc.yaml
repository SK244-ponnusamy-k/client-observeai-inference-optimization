# Metadata cache PVC — tokenizer + config files only.
# Model weights stream from S3 via Run:ai streamer — NOT stored here.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: oai-infopt-metadata-qwen-0.5b
  namespace: oai-infopt
  labels:
    app.kubernetes.io/name: oai-infopt-metadata-qwen-0.5b
    app.kubernetes.io/component: metadata-cache
    project: observeai-inference-optimization
    engagement: shellkode-sow
    environment: sandbox
    model: qwen-2.5-0.5b
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gp3
  resources:
    requests:
      storage: 5Gi
