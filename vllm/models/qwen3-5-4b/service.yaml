# GENERATED by `oai model generate qwen3-5-4b` - edit catalog/models/qwen3-5-4b.yaml instead.
apiVersion: v1
kind: Service
metadata:
  name: oai-infopt-vllm-qwen3-5-4b
  namespace: oai-infopt
  labels:
    app.kubernetes.io/name: oai-infopt-vllm-qwen3-5-4b
    app.kubernetes.io/component: inference-server
    project: observeai-inference-optimization
    engagement: shellkode-sow
    environment: sandbox
    model: qwen3-5-4b
spec:
  # ClusterIP only - internal cluster access via benchmark runner or port-forward.
  # No public LoadBalancer. Use ALB Ingress with auth for external access.
  type: ClusterIP
  selector:
    app: oai-infopt-vllm-qwen3-5-4b
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
