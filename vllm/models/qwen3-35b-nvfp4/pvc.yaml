# Metadata cache PVC — tokenizer + config files only.
# Model weights stream from S3 via Run:ai streamer — NOT stored here.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: oai-infopt-metadata-qwen3-35b-nvfp4
  namespace: oai-infopt
  labels:
    app.kubernetes.io/name: oai-infopt-metadata-qwen3-35b-nvfp4
    app.kubernetes.io/component: metadata-cache
    project: observeai-inference-optimization
    engagement: shellkode-sow
    environment: sandbox
    model: qwen3-35b-nvfp4
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gp3
  resources:
    requests:
      storage: 5Gi
